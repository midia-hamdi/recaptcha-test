import React from 'react';
import ReactDOM from 'react-dom';
import SolveMedia from './SolveMedia';

const root = document.getElementById('root');
ReactDOM.render(
  <React.StrictMode>
    <SolveMedia />
  </React.StrictMode>,
  root
);
